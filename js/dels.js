/*const dels = document.querySelectorAll('.delete');
for (i=0; i<dels.length; i++)
{
	addEvent( dels[i], 'click', function(e){
	//dels[i].addEventListener( 'click', function(e){
		e.preventDefault();
//		const tr_trig = this.parentElement.parentElement.parentElement;
//		const modal_btns = document.getElementsByClassName('modal-footer')[0];
//		addEvent( modal_btns, 'click', modalBtnClicked );
//		addEvent( modal.footer, 'click', modalBtnClicked );
	});
}

function modalAction_deletet(btn)
{
	// this => <div class="modal-footer">…</div>
	// e.target => <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
	console.log(this);
}
function modalBtnClicked(e)
{
	// this => <div class="modal-footer">…</div>
	// e.target => <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
	console.log(this);
}*/
